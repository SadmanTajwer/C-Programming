#include<stdio.h>
#include<math.h>
main()
{
    int x,s,C=0,Y=0;
    scanf("%d",&x);
    s=x%2;
    if (s==0)
   { (Y=x+1);
    printf("%d",Y);
   }
    else if(s=!0)
    {(C=x+2);
    printf("%d",C);
    }
}
